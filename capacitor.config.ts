import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.licence3.projet_sn',
  appName: 'Employme',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
